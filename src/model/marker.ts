export class Marker {
  start_date: Date; // a Date object that sets the marker's date
  css: string;      // a CSS class applied to the marker
  text: string;     // the marker title
  title: string;    // the marker's tooltip
}